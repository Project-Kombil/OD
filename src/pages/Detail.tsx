import { Box } from "@mui/material";

import NavBar from "../components/NavBar";

function Detail() {
	return (
		<Box sx={{ flexGrow: 1 }}>
			<NavBar name="DETAIL" />
		</Box>
	);
}

export default Detail;
